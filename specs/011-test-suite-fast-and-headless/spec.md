# Feature: Fast, headless, hardware-safe test suite

Make `uv run pytest` fast, non-interactive, and never move a physical AxiDraw unless `-m hardware` is used explicitly.

See `research.md` for audit and `docs/testing.md` for commands.
